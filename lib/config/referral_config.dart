const referralInviterRewardTokens = 100;
const referralInviteeRewardTokens = 100;
const referralCodeLength = 8;

const referralSuccessInviteeMessage =
    'Referans ödülün tanımlandı: +100 jeton';
const referralCodeNotFoundMessage =
    'Referans kodu bulunamadı, kayıt normal devam etti.';
const referralInviterRewardMessage =
    'Arkadaşın kayıt oldu, +100 jeton kazandın.';
